repositories {
   flatDir {
       dirs '../../../libs'
   }
}

dependencies {
	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
    compileOnly name: "com.liferay.postcodes.api-1.0.0"
}